#pragma once

/*
	This should be included by any applications that use Arcane
	Application contains a function that needs to be implemented by the client
	Then EntryPoint can call the client defined function and we can startup the engine
*/

#include "Core/Application.h"
#include "EntryPoint.h"
